# React Router Dom 
 
 1. type of Router in react 

    BrowserRouter -> used for browser base application which store data in browser
    HistoryRouter
    MemoryRouter ->use in react native applications , 
    HashRouter -> A declarative <Router> that stores the location in the hash portion of the URL so it is not sent to the server.
